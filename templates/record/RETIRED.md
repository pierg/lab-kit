# Retired — LIVE

**Git is the archive; the working tree holds only what should be read today.** Superseded documents are deleted, not moved to an `archive/` directory for the next reader to re-triage. This file is how they are recovered.

Exactly two things survive being wrong *in place*: a row in `findings.md` (struck through, because it may have been cited) and a claim on a page that already circulated.

| Date | What was removed | Why | Recover at |
|---|---|---|---|
