# F-3 — defense — LIVE

**Status: LIVE** — the defense of finding `F-3` (banked 2026-09-06); its interface row is `F-3` in `record/findings.md`.

## Predictions as scored

P1 (E2's pre-registration): "insertion sort wins most seeds below 16 elements", confidence 60% — **miss**. The kill rule (fewer than half the seeds at 8 elements) fired: 27 of 200.

## The number

At 8 elements insertion sort averages 19.0 comparisons against 15.8; at 4 elements the two are level (4.8 against 4.7). From 16 elements up it wins no seed of 200 (`out/summary.tsv`).

## Why the number is not something else

Libraries switch to insertion sort for small arrays because of cheap moves and good locality, not fewer comparisons. This row measures comparisons only, so it does not contradict that practice; it only closes the comparison question.
