# F-1 — defense — LIVE

**Status: LIVE** — the defense of finding `F-1` (banked 2026-09-03); its interface row is `F-1` in `record/findings.md`.

## Predictions as scored

P1 (E1's pre-registration): "insertion sort wins at least 15 of 20 seeds at 1% displaced", confidence 70% — **hit**, 18 of 20.

## The number

The mean over 20 seeds at 1% displaced is 4044.6 comparisons for insertion sort and 5966.2 for merge sort (`out/summary.tsv`, row `0.01`). The two seeds it loses are the ones whose swaps moved an element far across the array, where insertion sort pays per position moved.

## Why the number is not something else

Displacement is by random swaps of two positions; a different disorder model (adjacent swaps, a reversed run) would move the crossover. The metric is comparisons, so the row says nothing about time.
