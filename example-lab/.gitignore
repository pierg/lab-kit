# kit/ and .claude/ are installed by `make example` (lab-kit's install.sh with content-kit's engine),
# and the indices are regenerated by it — this example commits only what a lab authors.
kit/
.claude/
content/catalog.json
content/search-index.json
content/backlinks.json
content/chronicle.json
content/ladder.json
content/*/*/nav.json
dist/
.serve.pid
.serve.log
__pycache__/
*.pyc
